feat: Implement IndexedDB for image storage

Adds the core functionality for storing images in IndexedDB. This includes:
- `initDB()`: To set up the database and the "images" object store.
- `saveImage(id, blob)`: To save an image.
- `getImage(id)`: To retrieve an image.
- `deleteImage(id)`: To remove an image.

This is the first step towards implementing image support in notes.
